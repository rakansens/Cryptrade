# 認証機能テストガイド

## 実装状況

### ✅ 完了した項目
1. **エラー修正**
   - 不足パッケージのインストール（@vercel/kv, @upstash/redis）
   - Labelコンポーネントの作成
   - Cookieパースエラーの解消（Supabase SSRへの移行）

2. **テストログイン機能**
   - 開発環境でのワンクリックテストログインボタン
   - テストアカウント情報：
     - Email: `test@example.com`
     - Password: `testpass123`

3. **userIdのnullチェック**
   - `auth-handler.ts`でsession.userの存在確認を追加
   - 全APIエンドポイントで適切なnullチェックを実装

4. **データベース接続**
   - テストユーザーが正常に作成され、Prismaでの接続を確認
   - Supabase AuthとPrismaデータベースの同期を確認

## テスト手順

### 1. アプリケーションの起動
```bash
npm run dev
```

### 2. ログインページへアクセス
- http://localhost:3000/login

### 3. テストアカウントでログイン
以下のいずれかの方法でログイン：

#### 方法1: ワンクリックログイン（開発環境のみ）
- 「テストアカウントでログイン」ボタンをクリック
- 自動的にテストアカウント情報が入力される
- 「ログイン」ボタンをクリック

#### 方法2: 手動入力
- Email: `test@example.com`
- Password: `testpass123`

### 4. 認証後の動作確認
- ログイン成功後、自動的に`/dashboard`へリダイレクト
- ダッシュボードでユーザー情報が表示される
- ログアウトボタンが機能する

## 保護されたルートのテスト

### 認証が必要なページ
- `/dashboard` - ユーザーダッシュボード
- `/api/auth/me` - ユーザー情報API

### 公開ページ
- `/` - ホームページ
- `/login` - ログインページ
- `/signup` - 新規登録ページ
- `/reset-password` - パスワードリセット
- `/api/binance/*` - 公開API

## トラブルシューティング

### エラー: "Failed to parse cookie string"
このエラーが表示される場合は、ブラウザのCookieをクリアしてください。

### エラー: "Unauthorized - Please login"
セッションが期限切れの可能性があります。再度ログインしてください。

### テストアカウントでログインできない場合
```bash
# テストユーザーを再作成
npx tsx scripts/create-test-user.ts
```

## セキュリティ考慮事項

1. **開発環境のみの機能**
   - ワンクリックテストログインは本番環境では無効
   - `NODE_ENV=development`の場合のみ表示

2. **本番環境への移行**
   - 環境変数の設定を確認
   - テストアカウントを削除
   - 適切なCORS設定

## 次のステップ

1. **E2Eテストの追加**
   - Playwrightを使用した認証フローのテスト

2. **追加機能の実装**
   - メール確認フロー
   - ソーシャルログイン
   - 多要素認証（MFA）